<x-app-layout>
    {{-- <x-slot name="header">
        <h2 class=" font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
            <a href="{{route('products.index')}}" class="mr-10">Productos</a>
            <a href="{{route('users.index')}}" class="mr-10">Clientes</a>
            <a href="{{route('suppliers.index')}}" class="mr-10">Proveedores</a>
            <a href="{{route('purchases.index')}}" class="mr-10">Compras</a>
            <a href="{{route('sales.index')}}" class="mr-10">Ventas</a>
        </h2>
    </x-slot> --}}

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-xl sm:rounded-lg">
            </div>
        </div>
    </div>
</x-app-layout>
